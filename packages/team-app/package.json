{
  "name": "@meetmgr/team-app",
  "version": "0.3.38",
  "private": true,
  "description": "Web app for team registration, entries, and invoices",
  "scripts": {
    "dev": "docker compose up -d",
    "build": "docker compose build",
    "stop": "docker compose down"
  }
}
