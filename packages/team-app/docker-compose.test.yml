# Test overlay — exposes backend on port 8000 for integration tests
# Usage: docker compose -f docker-compose.yml -f docker-compose.postgres.yml -f docker-compose.test.yml --env-file tests/test.env up --build -d

services:
  backend:
    ports:
      - "8000:8000"
