# TODO

## Pending

- [x] **Deprecate SMB import/export in team-app** — Investigated and removed. See [`docs/SMB_DEPRECATION_INVESTIGATION_2026-08-03.md`](./SMB_DEPRECATION_INVESTIGATION_2026-08-03.md) for the full rationale (confirmed by the author: added early on before team-app's schema diverged from Splash's; fully superseded by LXF, including Gemini-key transport). Removed `smb.py`/`smb_to_team.py`/`generate_smb.py`, the three endpoints (`upload/meet-smb`, `export/meet-smb`, `admin/import-meet-results`), the Admin.jsx upload/download/import UI, the `.smb` tests (`tests/unit/test_smb.py` + the destructive classes in `test_integration.py`), and fixed the stale Gemini-key line in `team-admin_{fr,en}.md`.

- [ ] **Beach event template with combined events** — Finish the beach event template (`config/template_beach.lxf`) complete with combined event definitions. This includes defining the cumulative point standings categories for beach disciplines (swimstyleids 601-605) so that the combined events generator can produce the correct XML for beach meets. *(Owner: Marianne)*

- [ ] **SMB restore fails on Splash: schema drift + missing tables** — `DSQITEM.OPTIONS` overflow, `RELAY.ATHLETES`/`RELAYCODE` width, and a `RELAYSPLIT` table silently dropped from every export are fixed and confirmed live (DSQ codes + combined-events points now load correctly in real Splash). A follow-up crash opening the Results module on a beach meet (access violation in `TModulResultFactory.HandleNotification`) survived **four** rounds of fixed-and-kept-but-ruled-out hypotheses (`HEAT.AGEGROUPID` nulls, missing `RECORD*` tables, `SWIMSTYLE` stroke/technique/code encoding, `HEAT.RACESTATUS` 4-vs-2 — see audit doc for detail) plus a fifth batch not yet live-tested (`AGEGROUP.AFINAL8LANES`, `RECORDAGEGROUP` sentinel row, beach `MEETVALUES.COURSE`). **Decisive result from a shared-Postgres control test**: meet-app and Splash both connected to the same live Postgres database, no `.smb` involved — with Splash initializing the schema first, meet-app writing (import/heats/results) second, Splash reopening third, **it works, no crash**. This confirms the Results-module crash is specific to something in the `.smb` export path (`smb.ts`) — not meet-app's underlying data model. So the fifth batch of fixes above is now the live one to test directly by re-exporting `.smb` and restoring, rather than continuing to guess from the crash trace. Getting to this result took several detours (Postgres write-order matters — meet-app-writes-first genuinely breaks Splash reopening the DB at all, a separate, expected constraint, not a bug — plus a string of real SQLite-only-syntax bugs blocking the control test itself, and a `MEET_TYPE` auto-detect gap when Splash does the import instead of meet-app — all listed below, all fixed). Full audit, blow-by-blow findings, and fix checklist: [`docs/SMB_SCHEMA_AUDIT_2026-08-01.md`](./SMB_SCHEMA_AUDIT_2026-08-01.md).

- [x] **Entries import against Postgres hit a string of SQLite-only-syntax bugs** — found while setting up the shared-Postgres control test above; all real, unambiguous, confirmed via actual Postgres error logs (not guesswork). `generateBeachNumbers()` (`beachNumber.ts`) had `SELECT DISTINCT ... ORDER BY <expr not in select list>` and SQLite's `COLLATE NOCASE` (also found a third `COLLATE NOCASE` in `db.ts`'s entries-by-event report) — fixed, and added PG-parity test coverage for `beachNumber.ts` (had none before), verified 39/39 against a real running Postgres instance. Then hit `INSERT OR IGNORE`/`INSERT OR REPLACE` (SQLite-only, no Postgres equivalent) three more times — `lenex.ts`'s relay-position import, `index.ts`'s DSQ-code seeding, and two `bsglobal` live-push-config upserts — fixed with plain `INSERT`/standard `ON CONFLICT ... DO UPDATE` (works on both backends, no branching needed). Then hit a fourth, different-shaped bug: `seedDsqCodes` writing the long `dsqitem.options` tag (up to 17 chars) into a column Postgres enforces as `VARCHAR(5)` (deliberately narrow, to match real Splash's own schema for live DB sharing) — fixed by compacting to single-letter codes (`compactDsqOptions()`) that still carry the individual/relay meaning `HeatsPage.tsx`'s DSQ dropdown filter needs (unlike the earlier `.smb`-export fix for the same field, this one's actually read at runtime, so couldn't just blank it). `lenex.ts` still has no PG-parity test coverage at all (same gap `beachNumber.ts` had) — not closed this pass, worth doing properly later.

- [x] **Connecting to a Postgres database Splash (not meet-app) populated showed as a pool meet** — `importLenex`'s `MEET_TYPE` auto-detect (`swimstyleid >= 600` → beach) only ran during meet-app's own LXF import; connecting to a database someone else already populated skipped it entirely, silently defaulting to pool. Fixed: `connectToPg` (`connectionManager.ts`) now runs the same auto-detect on connect whenever `MEET_TYPE` isn't already set in `bsglobal`. **Confirmed live.**

- [x] **`.smb` export failed on an existing database: `no such column: afinal8lanes`** — that column was added to `schema.ts` a session before this one, but `CREATE TABLE IF NOT EXISTS` never backfills a new column onto a table that already exists, so it silently never reached any pre-existing meet-app database. Added a small, generic `runColumnBackfills()` mechanism (`schema.ts`) instead of a one-off fix, wired into both the SQLite and Postgres connect paths, plus a regression test. Restart meet-app to pick it up (this is exactly the kind of fix that needs a fresh app launch, not just a re-export).
